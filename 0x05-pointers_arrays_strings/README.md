# 0x05. Pointers, Arrays and Strings

This project is part of the ALX Low Level Programming track.  
It introduces the concept of **pointers, arrays, and strings in C programming**.

## Learning Objectives

At the end of this project, I understand:

- What pointers are and how to use them
- How pointers and arrays work together
- How to manipulate strings in C
- How to pass variables by reference
- How memory addresses work in C

## Project Requirements

- Allowed editors: `vi`, `vim`, `emacs`, `nano`
- Compilation on Ubuntu 20.04 using:

- All files end with a new line
- Code follows **Betty style**
- No global variables
- Maximum **5 functions per file**
- No use of `printf` or `puts`
- Output handled using `_putchar`

## Files

| File | Description |
|-----|-------------|
| 0-reset_to_98.c | Updates the value of a pointer to 98 |
| 1-swap.c | Swaps the values of two integers |
| 2-strlen.c | Returns the length of a string |
| 3-puts.c | Prints a string followed by a new line |
| 4-print_rev.c | Prints a string in reverse |

## Author

- **Tajudeen Bawumia**
- ALX Software Engineering Program
- All files end with a new line
- Code follows **Betty style**
- No global variables
- Maximum **5 functions per file**
- No use of `printf` or `puts`
- Output handled using `_putchar`

## Files

| File | Description |
|-----|-------------|
| 0-reset_to_98.c | Updates the value of a pointer to 98 |
| 1-swap.c | Swaps the values of two integers |
| 2-strlen.c | Returns the length of a string |
| 3-puts.c | Prints a string followed by a new line |
| 4-print_rev.c | Prints a string in reverse |

## Author

- **Tajudeen Bawumia**
- ALX Software Engineering Program
